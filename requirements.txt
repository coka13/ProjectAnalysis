# Shared by both front ends: everything below the window.
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
cryptography>=42.0
networkx>=3.2
PyYAML>=6.0

# Then pick an interface (both may be installed side by side):
#   pip install -r requirements-webview.txt   WebView2 build
#   pip install -r requirements-native.txt    native build, no browser engine
